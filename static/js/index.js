// Script para a página principal (pode ser expandido conforme necessidade)
console.log('Página principal carregada');

// Exemplo de função que pode ser adicionada posteriormente
function checkAuth() {
    // Verificar se o usuário está autenticado
    // Se não estiver, redirecionar para login
}